const { buttons } = require('./keyboard-buttons')

const keyboard = [
  [buttons.kamensk, buttons.karpinsk],
  [buttons.krasnoturink, buttons.krasnouralsk],
  [buttons.pervouralsk, buttons.pishma],
  [buttons.polevskoi, buttons.revda],
  [buttons.serov, buttons.tagil],
  [buttons.back],
]

module.exports = { keyboard }
