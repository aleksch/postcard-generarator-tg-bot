const {cityScene} = require('./city')
const {startScene} = require('./start')
const {effectScene} = require('./effects')
const {textScene} = require('./text')
const {analyticsScene} = require('./analytics')


module.exports = {

    cityScene,
    startScene,
    effectScene,
    textScene,
    analyticsScene
}